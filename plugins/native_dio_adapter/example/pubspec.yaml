name: native_dio_adapter_example
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
#  dio: ^5.0.0
  native_dio_adapter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:
  http:
    path: ../../../../http/pkgs/http/
  cronet_http:
    path: ../../../../http/pkgs/cronet_http/
  cupertino_http:
    path: ../../../../http/pkgs/cupertino_http/
  dio:
    path: ../../../../dio/dio/

flutter:
  uses-material-design: true